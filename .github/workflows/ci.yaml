name: Java chat CI

on: push

jobs:
  backend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Build/test with Maven
        run: |
          mvn package -P no-client-tests

  client-chat-form:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="ChatFormTest" -pl client

  client-configure-chat-form:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="ConfigureChatFormTest" -pl client

  client-create-chat-form:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="CreateChatFormTest" -pl client

  client-forgot-password-form:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="ForgotPasswordFormTest" -pl client

  client-forward-message-form:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="ForwardMessageFormTest" -pl client

  client-login-form:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="LoginFormTest" -pl client

  client-profile-form:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="ProfileFormTest" -pl client

  client-properties:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package -Dtest="PropertiesTest" -pl client

  system:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '19'
          distribution: 'corretto'
          cache: 'maven'
      - name: Set up Xvfbc
        run: |
          sudo apt-get update
          sudo apt-get install xvfb
          sudo apt-get install at-spi2-core
      - name: Build/test with Maven
        run: |
          Xvfb :99 &
          export DISPLAY=:99
          mvn package test -P system